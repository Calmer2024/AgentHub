# Spec Template: [模块名称]

**版本**: v1.0
**创建日期**: YYYY-MM-DD
**状态**: Draft | In Development | Completed
**关联 ADR/PRD**: [ADR-XXXX]、[PRD-XX]
**依赖模块**: [Phase X 模块名]

> 本模板是 AI 生成代码的第一输入源。每个章节的信息密度必须足以让 AI 不加猜测地完成实现。任何两个 AI Agent 拿到同一份 Spec，产出的代码应在所有可观察行为上一致。

---

## 1. 目标

> 1-2 段话：本模块解决什么问题？目标用户是谁？为什么需要它？

**成功标准**（可证伪）：

- [ ] [可测量的量化标准，如："用户从输入到看到首 token < 3 秒"]
- [ ] [可观察的行为标准，如："产物卡片在 artifact.created 后 1 秒内出现在聊天流"]
- [ ] 不通过标准：[明确什么情况算"没做完"]

---

## 2. 全局定位

### 2.1 北极星链路位置

```text
[上游模块]  →  [本模块]  →  [下游模块]
```

### 2.2 上下游契约

| 方向 | 模块/事件/API | 本模块的角色 |
|------|-------------|------------|
| **上游输入** | [谁调本模块？什么事件/API 触发？请求体字段？] | |
| **下游产出** | [本模块产出什么？什么事件/API/UI 状态？下游谁消费？] | |
| **本模块不通** | [明确哪些链路不在范围内，由后续哪个模块补齐] | |

---

## 3. 跨模块契约

> **本章只定义跨模块边界的东西——别人需要知道的。** 模块内部实现（内部类、算法、中间变量、正则表达式）由 AI 自由决定，不写入 Spec。
>
> 判断标准：如果改了它，另一个模块会坏 → 写入本章。否则 → AI 自己决定。

### 3.1 API 端点

| 端点 | 方法 | 请求体 | 成功响应 | 错误响应 |
|------|------|--------|---------|---------|
| `/api/xxx` | POST | `{ "field": type }` | `200: { "field": type }` | `400: { "error": "msg", "detail": "..." }` / `404` |

### 3.2 事件

| 事件类型 | 方向 | payload 字段 |
|---------|------|-------------|
| `artifact.detected` | Adapter → EventBus | `{ sessionId, agentId, artifactType: "code_diff" | "web_preview" | "file_tree", content }` |
| `artifact.created` | ArtifactService → EventBus | `{ artifactId, sessionId, version }` |

### 3.3 数据库 Schema 变更

```sql
-- 仅列出对外可见的表/列变更。内部索引、中间表由 AI 决定。
ALTER TABLE sessions ADD COLUMN project_id VARCHAR NOT NULL REFERENCES projects(id);
```

### 3.4 跨组件 TypeScript 类型（前端）

```typescript
// 仅在多个组件间共享的 Props / Store 类型
interface ArtifactCardProps {
  artifactId: string;
  sessionId: string;
  // ...
}
```

---

## 4. 行为规格

### 4.1 正常流程

> 按步骤描述理想情况的完整执行流程。每一步标注触发者和产出。

```
1. [触发者] 动作 → [系统] 响应 → [产出]
2. ...
```

### 4.2 UX 六态覆盖

> 每个 UI 组件 / API 端点必须覆盖以下 6 个状态。缺任一态 = Spec 不完整。

| 状态 | 用户看到什么 | 触发条件 |
|------|------------|---------|
| **空态** (Empty) | [无数据时的界面，不是白屏] | |
| **加载态** (Loading) | [skeleton / spinner / 进度条 / 打字机] | |
| **正常态** (Normal) | [数据正常展示的完整界面] | |
| **完成态** (Complete) | [操作完成后的确认/跳转/更新] | |
| **错误态** (Error) | 见 §4.3 | |
| **边界态** (Edge) | [空输入/极长输入/并发/快速切换等] | |

### 4.3 错误处理

| 错误场景 | 错误码 | 用户可见文案 | 恢复路径 |
|---------|--------|------------|---------|
| [场景] | [HTTP 码或内部码] | [前端显示什么文字] | [重试/取消/降级/联系支持] |

---

## 5. 前端页面设计

> **本章是 AI 生成前端布局和组件的静态结构输入。** 描述页面的视觉布局、组件树、区域划分和关键视觉元素。交互行为见 §6。

### 5.1 页面布局

用 ASCII 图描述页面整体布局结构，标注每个区域的功能和对应的前端组件名：

```
┌──────────┬───────────────┬──────────────────────────┐
│ 区域 A    │ 区域 B         │ 区域 C                    │
│ (组件名)  │ (组件名)       │ (组件名)                  │
│          │               │                          │
│ 具体     │ 具体          │ 具体                     │
│ 内容     │ 内容          │ 内容                     │
└──────────┴───────────────┴──────────────────────────┘
```

每个区域说明：宽度（px 或比例）、滚动行为、响应式折叠规则（如果有）。

### 5.2 组件树

```
App
├── LayoutShell
│   ├── ProjectSidebar (§5.1 区域 A)
│   │   ├── FriendList
│   │   │   └── FriendEntry[]       ← 每个 CLI Agent
│   │   └── ProjectList
│   │       └── ProjectItem[]       ← 每个 Project
│   ├── SessionSidebar (§5.1 区域 B)
│   │   ├── SessionSearchBar
│   │   ├── SessionList
│   │   │   └── SessionItem[]       ← 每个对话
│   │   └── CreateChatButton
│   └── ChatView (§5.1 区域 C)
│       ├── ChatHeader
│       ├── MessageList
│       │   ├── UserMessageBubble[]
│       │   ├── AgentMessageBubble[]
│       │   └── ArtifactCard[]
│       └── MessageInput
```

### 5.3 关键视觉元素

| 元素 | 位置 | 视觉规格 |
|------|------|---------|
| [元素名] | [在哪个区域/组件下] | [尺寸、颜色、图标、字体、间距] |
| [如：好友头像] | ProjectSidebar > FriendEntry | 36x36px 圆形、CLI 工具 logo、右侧 8px 状态点（绿/灰/红） |
| [如：Artifact Card] | ChatView > MessageList | 圆角 8px 白色卡片、左边类型图标、加粗标题、右侧操作按钮 |

---

## 6. 前端交互序列

> **本章是 AI 生成前端交互逻辑的核心输入。** 用"用户动作 → 系统响应 → 界面变化"的序列描述每个交互流程，精确到组件名、事件类型、视觉变化。静态布局见 §5。

```
用户: [具体动作，如"在聊天流中 hover 某条 AI 消息"]
  → 前端: [组件名] 显示 [UI 元素，如"消息右侧出现三个图标按钮：↩ 引用 / 🔄 重新生成 / 📌 钉选"]
  → 用户: [下一步动作，如"点击 📌"]
  → 前端: [API 调用，如 POST /api/messages/{id}/pin]
  → 后端: [处理逻辑]
  → SSE/WebSocket: [事件类型，如 message.updated]
  → 前端: [Store 更新 + 组件重渲染，如 chatStore 更新 message.isPinned，消息顶部出现 📌 标记]
```

### 6.2 [如有多个交互流程，逐个编号]

---

## 7. 验收标准

> **每条必须可独立验证**。验证者不需理解产品意图就能判断通过/失败。

- [ ] AC-01: [可观察条件，如"创建新会话后，会话列表第一条显示新会话名称"]
- [ ] AC-02: [后端契约条件，如"POST /api/sessions 返回 201 + session id + created_at"]
- [ ] AC-03: [前端状态条件，如"发空消息点击发送 → 按钮置灰、无请求发出"]
- [ ] AC-04: [事件条件，如"agent.output 事件在 SSE 流中逐 chunk 到达前端"]

---

## 8. 测试策略

### 8.1 单元测试 ([N] 条)

| 测试对象 | 条数 | 覆盖内容 |
|---------|------|---------|
| [模块/类名] | N | [核心逻辑、边界值、异常路径] |

### 8.2 集成测试

- [测试场景：Mock X + 真实 Y → 验证全流程]

### 8.3 E2E 测试

- [测试场景：真实浏览器 → 用户操作序列 → 验证 UI 结果]

---

## 9. 架构约束追溯

> **AI 不得自行发明架构方案。** 每个关键设计决策必须追溯到一个已有的 ADR 或 PRD。如决策不属于任何已有文档，应先补充 ADR 再写 Spec。

| 本模块的决策 | 依据 |
|------------|------|
| [如："CLI Agent cwd = Project.workspace_path"] | ADR-0009 §核心规则 3 |
| [如："Adapter 不直接写数据库"] | PRD-01 §3.4 |
| [如："通过 EventBus 而非直接调用"] | ADR-0005 §依赖规则 2 |

---

## 10. 依赖

| 依赖模块 | 需要的接口 | 当前状态 |
|---------|-----------|---------|
| [模块名] | [具体接口/类/事件名] | ✅ 已就绪 / 🔧 开发中 / ❌ 未开始 |

---

## 11. Non-Goals（明确不做什么）

> 防止 AI 过度实现。明确列出本模块**不负责**的事项。

| 不做的事 | 原因 | 由谁负责 |
|---------|------|---------|
| [如："不做 Artifact Card 的 Drawer 预览"] | 下游链路 | Phase 7 Drawer 模块 |
| [如："不做 Docker 沙箱隔离"] | P2 范围 | P2 SaaS 版 |

---

## 12. 破坏性变更与迁移

> 如果本模块是对已有功能的修改（V2 覆盖 V1），必须填写本章。全新模块可删除。

| 维度 | V1 行为 | V2 行为 | 迁移路径 |
|------|--------|--------|---------|
| [API/DB/组件] | [旧] | [新] | [数据迁移脚本/前端适配步骤/兼容期] |

> **版本历史**
> - v1.0 (YYYY-MM-DD): 初始版本
