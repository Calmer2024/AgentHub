# Spec Template: [模块名称]

**版本**: v1.0
**创建日期**: YYYY-MM-DD
**状态**: Draft | In Development | Completed
**关联 ADR**: [相关 ADR 编号]

---

## 1. 目标

> 1-2 句话说明：这个模块解决什么问题？谁会用？为什么需要它？

---

## 2. 全局链路定位

说明本模块位于 AgentHub 北极星链路的哪一段：

```text
用户输入 -> Orchestrator/Agent 执行 -> Artifact 创建 -> 聊天卡片 -> Drawer 预览 -> 编辑/审批回流
```

| 问题 | 回答 |
|------|------|
| 本模块接收哪个上游事件/API/用户动作？ |  |
| 本模块产出哪个下游事件/API/UI 状态？ |  |
| 完成后用户能独立完成什么任务？ |  |
| 本模块不打通哪些链路？ |  |

---

## 3. 输入输出

### 3.1 接口定义

```python
# 后端：API 端点或 Service 方法签名
# 前端：组件 Props 或 Store 接口
```

### 3.2 数据模型

```python
# 涉及的数据结构定义（Pydantic / TypeScript 类型）
```

---

## 4. 行为规格

### 4.1 正常流程（Happy Path）

> 按步骤描述理想情况下的执行流程。
> 例如：1. 用户点击发送 → 2. 消息出现在聊天窗口 → 3. 后端接收 → 4. 调用 Agent → 5. 流式返回 → 6. 前端逐字展示 → 7. 消息持久化

### 4.2 异常流程（Error Cases）

| 异常场景 | 预期行为 |
|----------|---------|
| [例如：API Key 未配置] | [前端显示明确错误提示，不是白屏或无限 loading] |
| [例如：网络中断] | [显示"连接失败"，支持点击重试] |
| [例如：Agent 返回超时] | [30 秒后显示超时提示，不阻塞后续操作] |

### 4.3 边界条件

> 空输入、极长输入、并发操作、快速切换等边界情况下应如何处理？

---

## 5. 验收标准

- [ ] [可手动验证的具体条件 1]
- [ ] [可手动验证的具体条件 2]
- [ ] [可手动验证的具体条件 3]
- [ ] [可手动验证的具体条件 4]

每条验收标准必须能对应一个用户可观察结果、后端契约/事件或明确测试用例。

---

## 6. 依赖

| 依赖模块 | 接口 | 状态 |
|---------|------|------|
| [模块名] | [需要调用的接口] | 已就绪 / 开发中 / 未开始 |

---

## 7. 不在范围内（Non-Goals）

> 明确列出本模块**不做什么**，防止范围蔓延。
> 例如：Phase 1 消息模块不做群聊、不做 WebSocket、不做产物预览卡片
